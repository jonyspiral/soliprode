# Antigravity Context: SoliProde

> **ESTE ARCHIVO ES EL GATILLO (TRIGGER) DE SESION.**
> Es un snapshot liviano del estado actual. No es memoria persistente.
>
> **Bootstrap:**
> 1. Leer este gatillo.
> 2. Leer `src/content/docs/protocolo-agentes.md`.
> 3. Leer `src/content/docs/docs-map.md`.
> 4. Leer la doc del área afectada en `src/content/docs/`.
>
> **Nota de etapa (2026-06-01):**
> El repositorio está en fase fundacional. Prioridad: consolidar base de producto, navegación, documentación y deploy antes de agregar auth, Supabase o lógica de negocio.
>
> **Al finalizar cada sesión:**
> 1. Actualizar este archivo con el estado actual.
> 2. Crear o actualizar el `.md` correspondiente en `src/content/docs/`.

---

## Estado Actual (2026-06-01)

### Última sesión (2026-06-01) - bootstrap inicial de SoliProde
- **Base Next.js lista** — app creada con Next.js App Router, TypeScript, Tailwind CSS, ESLint, `src/` y alias `@/*`.
- **Shell compartida nueva** — `src/components/app-shell.tsx` define header, navegación superior y bottom nav mobile para todas las rutas.
- **Landing propia activa** — `src/app/page.tsx` reemplaza el starter de Next con la propuesta inicial de SoliProde y copy del `Prode Mundial Solidario 2026`.
- **Rutas base creadas** — `/`, `/login`, `/dashboard`, `/matches`, `/rankings`, `/groups`, `/communities`, `/admin`.
- **PWA mínima** — `src/app/manifest.ts` define `manifest.webmanifest`.
- **Validación técnica** — `npm run lint` y `npm run build` pasaron OK.
- **Gobierno de agentes agregado** — se crean `AGENTS.md`, este gatillo, índice de planes y docs base para estandarizar trabajo futuro.
- **Remoto** — `origin` apunta a `https://github.com/jonyspiral/soliprode.git`, repo actualmente vacío antes de este primer push.

### Última sesión (2026-06-01) - base de Supabase preparada
- **Dependencia oficial instalada** — `@supabase/supabase-js` queda agregada al proyecto.
- **Configuración centralizada** — `src/lib/env.ts` y `src/lib/supabase/config.ts` resuelven URL, anon key y service role key.
- **Clientes base listos** — `src/lib/supabase/client.ts` y `src/lib/supabase/server.ts` preparan browser/server usage sin implementar auth.
- **Healthcheck mínimo** — `GET /api/health/supabase` consulta `/auth/v1/settings` para validar conectividad básica con el proyecto.
- **Docs alineadas** — `src/content/docs/supabase-base.md` documenta variables, archivos y próximo paso.
- **Pendiente real** — faltan `NEXT_PUBLIC_SUPABASE_ANON_KEY` y, si aplica, `SUPABASE_SERVICE_ROLE_KEY` en `.env.local` para usar la integración.

## Próximos pasos sugeridos

1. Publicar esta base a `main`.
2. Definir el primer plan funcional grande: auth, modelo de partidos o sistema de grupos.
3. Mantener docs y gatillo al día a medida que aparezcan nuevas áreas.
