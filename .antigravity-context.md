# Antigravity Context: SoliProde

> **ESTE ARCHIVO ES EL GATILLO (TRIGGER) DE SESION.**
> Es un snapshot liviano del estado actual. No es memoria persistente.
>
> **Bootstrap:**
> 1. Leer este gatillo.
> 2. Leer `src/content/docs/protocolo-agentes.md`.
> 3. Leer `src/content/docs/docs-map.md`.
> 4. Leer la doc del área afectada en `src/content/docs/`.
>
> **Nota de etapa (2026-06-01):**
> El repositorio está en fase fundacional. Prioridad: consolidar base de producto, navegación, documentación y deploy antes de agregar auth, Supabase o lógica de negocio.
>
> **Al finalizar cada sesión:**
> 1. Actualizar este archivo con el estado actual.
> 2. Crear o actualizar el `.md` correspondiente en `src/content/docs/`.

---

## Estado Actual (2026-06-01)

### Última sesión (2026-06-04) - refactor de Profile e identidad pública
- **Profile reordenado** — `/profile` ahora separa `Mi participación`, `Perfil de juego` y `Datos de cuenta`, con card principal mobile-first, avatar real de Google cuando existe e iniciales como fallback.
- **Sin migración nueva** — `profiles.public_alias` queda confirmado como nick de juego editable; `profiles.whatsapp` sigue como dato opcional de cuenta.
- **Acciones server-side nuevas** — `src/app/profile/actions.ts` actualiza nick de juego y WhatsApp, y revalida `Profile`, `Dashboard`, `Groups` y `Rankings`.
- **Helpers compartidos** — `src/lib/player/identity.ts` centraliza display name, avatar, estado de participación y validaciones básicas para evitar drift entre pantallas.
- **Identidad pública alineada** — Ranking, Team y Home vuelven a leer el nombre visible del jugador desde el helper compartido en vez de repetir `public_alias` crudo.

### Última sesión (2026-06-03) - QA users + inscripción online controlada
- **Usuarios QA documentados** — `src/content/docs/qa/test-users.md` documenta cuentas `qa+nonpaid`, `qa+pending` y `qa+paid`, sin secretos en repo.
- **Script local idempotente** — `scripts/qa/prepare-test-users.mjs` crea/actualiza `auth.users`, `profiles` y `participations` de QA usando `SUPABASE_SERVICE_ROLE_KEY`, con opción segura para limpiar pronósticos QA.
- **Logout robusto** — la salida desde `Profile` ahora fuerza cierre local + cleanup server-side + redirección dura a `/`.
- **Copy no paid limpiado** — el CTA principal visible pasa a `Completar inscripción` y `Mercado Pago` queda como apoyo, no como título principal del flujo.
- **Estados compartidos** — `src/lib/participations/status.ts` centraliza `paid`, `pending review` y `nonpaid` para Dashboard, Profile y shell.
- **Bloqueo productivo identificado** — el checkout online en producción hoy responde `503` desde `/api/payments/mercadopago/create-preference` con `El pago online todavia no esta configurado.`, lo que apunta a `MERCADOPAGO_ACCESS_TOKEN` faltante o no cargado server-side.
- **Runbook agregado** — `src/content/docs/payments/mercadopago-production-checklist.md` deja checklist de envs, webhook, retornos y QA para habilitar checkout real sin activar `paid` desde el cliente.

### Última sesión (2026-06-03) - Home promo floor + countdown
- **Home orientada a conversión** — `/` ahora muestra `Pozo inicial` y `Jugadores` con piso comercial visible, sin exagerar métricas ni venderlas como tiempo real.
- **Piso centralizado** — `src/lib/product/home-display.ts` fija `INITIAL_PRIZE_POOL_ARS = 300000` y `INITIAL_PLAYERS_COUNT = 60`, y usa valores reales solo si superan esos pisos.
- **Countdown único de campaña** — `src/lib/product/promo-campaign.ts` centraliza `PROMO_END_AT` / `NEXT_PUBLIC_PROMO_END_AT` para una sola ventana visible que no depende de `localStorage` ni resetea por usuario.
- **Doc nueva** — `src/content/docs/product/home-promo-floor-and-countdown.md` deja la convención activa para Home.
- **Sin cambios fuera de alcance** — esta sesión no toca pagos server-side, Mercado Pago backend, Supabase schema, scoring, rankings, fixture, grupos, Admin, Dashboard ni Matches.

### Última sesión (2026-06-03) - roadmap de notificaciones PWA
- **Roadmap futuro documentado** — `src/content/docs/roadmap/pwa-notifications-roadmap.md` deja Push PWA, email y WhatsApp como fase futura.
- **No es MVP inmediato** — no implementar notificaciones hasta completar fixture, pronósticos, pagos, activación paid, ranking individual, ranking de grupos y Admin operativo.
- **Sin cambios funcionales** — esta sesión no toca UI, service worker, manifest, backend, Supabase, pagos, scoring, fixture ni rankings.

### Última sesión (2026-06-03) - estructura de fixture real
- **Migración de fixture creada** — `supabase/migrations/009_fixture_schema_hardening.sql` extiende `teams`, `matches` y `predictions` para grupos A-L, fechas `timestamptz`, cierre de pronósticos y aliases compatibles con el contrato nuevo.
- **Sin fixture falso** — no se cargan equipos, partidos, horarios ni resultados de prueba.
- **RLS endurecido** — los usuarios autenticados solo pueden insertar, editar o borrar sus propios pronósticos antes de `prediction_closes_at` y con partido `scheduled`.
- **Compatibilidad preservada** — `profile_id`, `predicted_home`, `predicted_away`, `score_home` y `score_away` siguen vigentes para el scoring y UI actuales; los campos nuevos quedan sincronizados por trigger.
- **Doc nueva** — `src/content/docs/database/fixture-schema.md` documenta equipos, partidos, pronósticos, permisos y futuros standings/bracket.
- **Import controlado preparado** — `scripts/fixture/import-fixture.mjs` + `src/content/docs/database/fixture-import.md` dejan lista la carga real por CSV con `dry-run` por defecto, validaciones fuertes y escritura manual explícita.

### Última sesión (2026-06-02) - contratos de tono, diseño y UI/UX
- **Contratos nuevos activos** — `src/content/docs/contracts/voice-contract.md`, `athletic-social-design-contract.md` y `ui-ux-implementation-contract.md` pasan a ser fuente de verdad obligatoria antes de tocar UI o copy.
- **Subplan de adopción creado** — `plans/soliprode-ui-contracts-implementation-plan.md` define fases, auditoría inicial, patrones base a proteger y orden futuro de polish visual.
- **Regla futura para prompts** — cualquier tarea de UI o copy debe revisar primero los tres contratos antes de editar pantallas o componentes.
- **Sin implementación funcional** — esta sesión no tocó backend, Supabase, pagos, scoring, rankings ni grupos.

### Última sesión (2026-06-02) - auditoría UI inicial contra contratos
- **Auditoría durable creada** — `src/content/docs/ui-audit-2026-06-02.md` documenta el estado actual de Home, Login, Register, Dashboard, Matches, Rankings, Profile y Admin.
- **Prioridades claras** — Dashboard, Matches y Rankings quedan como pantallas P0 para el próximo refactor visual incremental.
- **Hallazgos transversales** — se confirma exceso de explicación antes de acción, riesgo de métricas placeholder en dashboard y deuda menor de escalado en Mercado Pago.
- **Sin rediseño todavía** — esta sesión solo auditó y clasificó; no corrigió UI funcional.

### Última sesión (2026-06-02) - referencia visual Stitch documentada
- **Referencia visual nueva** — `src/content/docs/visual-reference/stitch-reference.md` traduce la imagen aprobada de Stitch a reglas concretas para Home, Register, Dashboard, Rankings y Matches.
- **Uso correcto definido** — la referencia se usa como guía de clima visual, jerarquía y proporciones, no como copia pixel-perfect ni permiso para inventar métricas o fixtures.
- **Prerrequisito para futuros slices UI** — antes de tocar UI deben revisarse contratos, auditoría y referencia Stitch juntos.
- **Sin cambios funcionales** — esta sesión no tocó páginas, componentes funcionales, backend, Supabase, pagos, scoring, rankings ni grupos.

### Última sesión (2026-06-02) - Home/Auth state-aware + logout visible
- **Home sensible a sesión** — `/` ahora cambia CTAs y copy según usuario no logueado, logueado no `paid` o logueado `paid`, sin insistir con registro o pago cuando ya no corresponde.
- **Auth con tono de juego** — `/login` y `/register` bajan el tono institucional y empujan Google como camino rápido, manteniendo email como alternativa.
- **Errores más humanos** — los mensajes de login/registro y callback evitan texto técnico crudo y contemplan el caso de confirmación de email del MVP.
- **Logout visible** — la shell muestra `Cerrar sesión` también en mobile y `dashboard` suma una salida clara dentro de `Tu cuenta`.
- **Atribución preservada** — `promoter_code` sigue pasando por query/cookie y no se pierde en login/register ni en OAuth.
- **Doc de Auth actualizada** — `src/content/docs/auth-google-setup.md` deja explícito dónde revisar `Confirm email` en Supabase para el MVP.

### Última sesión (2026-06-02) - Épica D de grupos competitivos
- **Grupo principal operativo** — `/groups` ya permite crear grupo y unirse con `invite_code`, usando `participations.group_id` como fuente de verdad del grupo principal.
- **Ranking interno real** — la página de grupos muestra miembros, excluye pendientes del ranking oficial y marca al `DT del grupo` como el activo con más puntos.
- **Ranking de grupos real** — se calcula promedio por grupo sobre puntos oficiales de jugadores `paid`, con estado `preview` vs `habilitado` por regla de `11+` activos.
- **Sin comunidades** — esta etapa no introduce membresías múltiples ni mezcla comunidades en la lectura o escritura de grupos.
- **Documentación nueva** — `src/content/docs/groups-competitive-mvp.md` deja las reglas y límites activos de esta competencia.

### Última sesión (2026-06-01) - bootstrap inicial de SoliProde
- **Base Next.js lista** — app creada con Next.js App Router, TypeScript, Tailwind CSS, ESLint, `src/` y alias `@/*`.
- **Shell compartida nueva** — `src/components/app-shell.tsx` define header, navegación superior y bottom nav mobile para todas las rutas.
- **Landing propia activa** — `src/app/page.tsx` reemplaza el starter de Next con la propuesta inicial de SoliProde y copy del `Prode Mundial Solidario 2026`.
- **Rutas base creadas** — `/`, `/login`, `/dashboard`, `/matches`, `/rankings`, `/groups`, `/communities`, `/admin`.
- **PWA mínima** — `src/app/manifest.ts` define `manifest.webmanifest`.
- **Validación técnica** — `npm run lint` y `npm run build` pasaron OK.
- **Gobierno de agentes agregado** — se crean `AGENTS.md`, este gatillo, índice de planes y docs base para estandarizar trabajo futuro.
- **Remoto** — `origin` apunta a `https://github.com/jonyspiral/soliprode.git`, repo actualmente vacío antes de este primer push.

### Última sesión (2026-06-01) - base de Supabase preparada
- **Dependencia oficial instalada** — `@supabase/supabase-js` queda agregada al proyecto.
- **Configuración centralizada** — `src/lib/env.ts` y `src/lib/supabase/config.ts` resuelven URL, anon key y service role key.
- **Clientes base listos** — `src/lib/supabase/client.ts` y `src/lib/supabase/server.ts` preparan browser/server usage sin implementar auth.
- **Healthcheck mínimo** — `GET /api/health/supabase` consulta `/auth/v1/settings` para validar conectividad básica con el proyecto.
- **Docs alineadas** — `src/content/docs/supabase-base.md` documenta variables, archivos y próximo paso.
- **Pendiente real** — faltan `NEXT_PUBLIC_SUPABASE_ANON_KEY` y, si aplica, `SUPABASE_SERVICE_ROLE_KEY` en `.env.local` para usar la integración.

### Última sesión (2026-06-01) - esquema inicial de base preparado
- **Migración inicial creada** — `supabase/migrations/001_initial_schema.sql` define `profiles`, `promoters`, `communities`, `groups`, `participations`, `teams`, `matches`, `predictions`, `bonus_predictions` y `rankings_cache`.
- **Índices y checks básicos** — la migración agrega índices prácticos, `created_at default now()`, `gen_random_uuid()` y triggers de `updated_at` donde corresponde.
- **RLS activado en todas las tablas** — con starter policies para lectura pública de `teams`, `matches`, `rankings_cache`, lectura propia autenticada y write propio de `predictions`.
- **Documentación nueva** — `src/content/docs/database-schema.md` resume relaciones, reglas y decisiones del esquema.
- **Fuera de alcance preservado** — todavía no hay seeds, integración UI, auth aplicada ni lógica real de rankings/pagos.

### Última sesión (2026-06-01) - policies mínimas para registro
- **Migración incremental creada** — `supabase/migrations/002_registration_policies.sql` agrega policies de registro para `profiles`, `participations`, `communities` y `groups`.
- **Ownership explícito** — perfiles, participaciones, comunidades y grupos quedan writeables solo por el usuario/owner autenticado correspondiente.
- **Visibilidad pública acotada** — `communities` y `groups` solo quedan visibles al público cuando `visibility = 'public'`.
- **Promotores resueltos con vista pública** — se agrega `public.active_promoters_public` para exponer solo `code` y `name` de promotores activos, porque RLS no restringe columnas.
- **Sin cambio funcional de UI** — no se tocó auth, frontend ni seed data.

### Última sesión (2026-06-01) - primer flujo real de Supabase Auth
- **SSR auth alineada** — `@supabase/ssr` queda instalada y `src/lib/supabase/server.ts` / `client.ts` pasan a usar clientes con cookies.
- **Middleware de sesión** — `src/lib/supabase/middleware.ts` y `src/middleware.ts` refrescan sesión y protegen `/dashboard`, además de evitar volver a `/login` o `/register` cuando ya hay sesión.
- **Registro real** — `src/app/register/actions.ts` crea usuario en Auth, guarda `profiles` y deja una `participation` en `pending`.
- **Login real** — `src/app/login/actions.ts` usa email/password y redirige al panel.
- **Dashboard protegido** — `src/app/dashboard/page.tsx` redirige a `/login` sin sesión y muestra perfil + estado de participación cuando el usuario está autenticado.
- **Campos futuros preservados** — promotor, comunidad y grupo quedan visibles en el registro, pero comunidad/grupo siguen marcados como próximos pasos y no crean datos todavía.

## Próximos pasos sugeridos

1. Auditar la UI actual página por página contra los contratos nuevos.
2. Priorizar refactor visual incremental sin abrir rediseño total.
3. Mantener docs, planes y gatillo alineados cada vez que cambie tono, UI o patrones base.
